# Atrox.Vectra.Runtime.Api
Is a transport-agnostic runtime service that executes configurable database procedures (SQL Server or PostgreSQL) and returns normalized result sets, output parameters, and diagnostics. It supports REST, AMQP, gRPC, and WebSocket endpoints through one shared execution core and configuration-driven engine/transport selection.
